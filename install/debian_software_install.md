# Debian Software install

## As root

### Update and install

```
apt-get update -y && apt-get upgrade -y
apt-get install -y requirements.txt
```

requirements.txt
---
sudo
vim
curl
zsh
ufw
portsentry
fail2ban
apache2
mailutils
---